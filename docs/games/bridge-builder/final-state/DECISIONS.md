# Decisions & Dissent Log — Bridge Builder final production state (v2)

**Run:** `bridge-builder-final-state-20260910` · **Role:** Synthesizer (project-software-development-team, host-native)
**Inputs adjudicated:** `BRIEF.md`; `v1/PRD.md`, `v1/TECHNICAL_DESIGN.md`, `v1/UX_USER_FLOW.md`; `critique/REVIEW.md` (CRIT-1…12, MAJ-1…26, MIN-1…21, plus REVIEW §4 "What's missing", §5 contradictions, §6 assumptions).
**Deliverable:** allocation of every critique item to a disposition, the exact instruction a writer must apply, the owner decisions that genuinely cannot be taken inside the spec, the epic-vs-child split, and the scope-lock verdict.

**Adjudication order used (highest first).** (1) BRIEF §2 locked constraints: deterministic TypeScript engine is the sole correctness authority; renderer presents view-models and emits only the closed bounded intent union; no manipulative engagement; no fabricated evidence; comparators are references only; privacy/safety; no duplication of existing Jira work. (2) `RENDERER_BOUNDARY.md` / `PHASER_RENDERER_CONTRACT.md` (v1.0.0) and the closed 8-member intent union are **frozen** — where a critique implies changing them the resolution is to record the gap and require a separate approved GAME issue, never to rewrite the contract in the spec. (3) Extend existing children; state when no child can carry something and then give a bounded draft scope, or declare an explicit non-goal. (4) The named practical tie-breakers (per-SC accessibility conformance statement; one session/time authority — engine-owned monotonic deadline with host-supplied visibility events; non-extendable return contract; one canonical scorecard list; one comparator registry table with per-entry status; host-shell/return-contract surface owned by extending GAME-171's AC). (5) Decisiveness: reject what is factually wrong or lock-incompatible.

**Labelling conventions used below.** `Ⓟ` = proposed threshold requiring ratification (never presented as an existing contract value); `⟨owner⟩` = owner decision/evidence-owner marker (see §6); `Q-01…Q-23` = the canonical scorecard dimension IDs (see CRIT-5); `BB-CONTRACT-1` / `BB-CONTENT-1` = the two bounded follow-ups sanctioned under O-8 and now tracked as GAME-294 / GAME-295; implementation and acceptance remain open; `F1…F7` = the named semantic-mirror facts (see MAJ-5/MAJ-25); `[existing]` / `[required work]` keep their v1 meaning and must keep their citations.

---

## 0. Disposition summary

| Bucket | Total | ACCEPTED | REJECTED | DEFERRED |
|---|---|---|---|---|
| Blockers (CRIT-1…CRIT-12) | 12 | 12 | 0 | 0 |
| Majors (MAJ-1…MAJ-26) | 26 | 26 (2 ACCEPTED-MODIFIED) | 0 | 0 |
| Minors (MIN-1…MIN-21) | 21 | 19 | 2 | 0 |

No blocker was rejected, because no blocker is factually wrong: each names a contradiction or an unowned, policy-critical behaviour that is real in the v1 text. No critique item is deferred **wholesale**; §4 lists the seven **partial deferrals** (branches of accepted items, plus REVIEW §4 items declared out of v1), each with a revisit condition. §5 records the nine critique recommendations whose *prescribed remedy* was partially overruled with reasons. Blockers CRIT-1, CRIT-3, CRIT-6, CRIT-7 and CRIT-10 are resolved by decisions that are now written into the epic text and into child ACs, so no blocker remains unresolved.

---

## 1. Resolved Blockers

| ID | Issue (short) | Disposition | Resolution + exact instruction a writer must apply | Rationale |
|---|---|---|---|---|
| CRIT-1 | Endless unclocked "Workshop" mode declared shipped, unowned, and never shown to be unreachable from the policy-bounded earned break | **ACCEPTED** | One name, one availability matrix, one non-goal. **PRD §3.8:** delete the sentence `Workshop is endless, unscored, unclocked`; replace with: *"The only unclocked mode in v1 is **Relaxed build** — an in-session toggle (bridges-built progress, no clock, stuck threshold doubled). There is **no** endless free-build shelf in v1: the previously proposed Workshop/Sandbox free-build mode is an **explicit non-goal** (§13) because it is owned by no child, is not evidenced as shipped anywhere in the repository ledger, and an unbounded unscored surface reachable from the earned-break host would breach the locked bounded-session constraint."* **PRD §9:** delete the `20-bridge sandbox session` memory budget (see MIN-4) and state the round-shaped protocol. **PRD §10:** remove "Workshop quality-of-life" from §12 nice-to-have. **PRD §2 / UX §1.4 / UX §2 J4 / UX §12 DQ-3 + GAP-2:** retire the names "Workshop" and "Sandbox" entirely; one canonical name (**Relaxed build**) and one explicit non-goal sentence. **Availability matrix (write once, in the epic text and in UX §1):** free site = timed round + Relaxed (child's own time); earned break = fixed 90 s window, Relaxed toggle **hidden**, **no** setup/summary/second-construction/unclocked surface reachable; lesson shell = break only. **GAME-135 AC:** add the runtime containment test (see §7.2) — a *test*, not a DOM string scan: from the break entry, no route, control, flag or URL parameter may reach setup, summary, "Play again"/"Change level", Relaxed, or any free-build surface. | A locked bounded session cannot coexist with an unowned endless mode whose reachability from the break is asserted nowhere; naming it once, capping it by construction and proving containment by test is the only way the epic can carry an enforceable engagement policy. Declaring the free-build shelf an explicit non-goal is preferred over inventing a story (rule 3) because no child, no evidence and no requirement support it. |
| CRIT-2 | "Ships combine/split" (PRD §3.3/§5) contradicts the frozen 8-intent union, TECH §6's additive-only placement, and UX DQ-5 | **ACCEPTED** | The verbs are removed, not added: combination/splitting is a change to legal actions and to the frozen contract, and PRD §4.1 itself requires a separate approved issue for that. **PRD §3.3:** delete `Extra verbs: **combine** two tray planks, **split** the last placed plank`; replace with: *"The intent union is exactly the frozen v1.0.0 set (`selectPiece, placePiece, removePiece, reset, submit, requestHint, continue, presentationComplete`). Combine, split, decompose and any other new verb are **out of contract**: adding one changes the legal-move set and requires a separate approved GAME issue with a contract re-version, engine legality/scoring changes and telemetry changes (PRD §4.1). It is a v1 non-goal (§13)."* **PRD §5:** delete the DragonBox justification sentence that rests on shipping combine/split (re-justify or demote per CRIT-11). **PRD §13:** add `combine/split/decompose verbs` to non-goals. **UX DQ-5:** close as decided ("out of v1; a new story if ever added"), not as an open question. | Shipping the verbs inside this spec would re-version a frozen contract with no owning issue and no legality semantics, and would silently change the legal-move set the exactness fixtures pin. Removing them keeps the contract, the engine algorithm and the fixtures consistent. |
| CRIT-3 | Header claims "every capability is owned / no new children"; the same document lists uncovered budgets, thresholds and the comparator decision; no action edits any child AC; Q1 has no owner or date | **ACCEPTED** | **PRD header (line 5):** delete `Every capability below is owned by an existing … child or a named cross-project dependency; no new children are proposed (final section)`. Replace with: *"Ownership is stated per area in the coverage check, together with the exact AC-edit actions required on GAME-134/135/166/171/172/39 and the two bounded new issues proposed in §11.6 (contract revision) and §4.2 (curriculum catalogue + content-volume supply floor). Areas that are epic text are labelled epic text."* **PRD coverage check:** replace `**Epic-text gap only**` dispositions with the ownership map plus the AC-edit list, and delete `### Genuine gaps requiring new Jira children: **none**` in favour of the honest list (§6.1/§6.2 of this log). **PRD §15 Q1:** convert from an open question into a dated owner-decision request (O-7, §6) recorded in GAME-105's DoD. **PRD §11.6:** replace `23-dimension scorecard` with the canonical `Q-01…Q-23` IDs (CRIT-5). | A deliverable that declares itself the epic's source of truth cannot simultaneously assert total coverage and label its own budget, threshold and comparator decisions uncovered; the fix is to publish the AC-edit actions and the owner decisions rather than the claim. |
| CRIT-4 | "WCAG 2.2 AA for the whole game surface" is unfalsifiable for a fixed-clock surface with no adjustment mechanism | **ACCEPTED** | Replace the blanket claim with a **per-success-criterion conformance statement** naming exceptions; do **not** remove the clock (locked). **PRD §8 opening line:** replace with: *"Target: **WCAG 2.2 AA**, asserted **per success criterion** in the conformance table in the epic text (one row per applicable SC: met / not met / not applicable, with evidence and, where not met, the claimed exception). Two criteria are **not met by design**: **2.2.1 Timing Adjustable** and **2.2.2 Pause, Stop, Hide** — the earned-break window is fixed by the host return-contract and the free round's cap is the product's honest session shape; the **essential-limit exception** is claimed and recorded for both, and **Relaxed build is the untimed conforming path** on the free site (surfaced on the S0 setup screen and in the hub card's secondary line, so it is discoverable without adult coaching). The conformance statement is signed by the owner-named accessibility signer (O-7). No document may claim blanket AA."* **CAREFUL — do not "fix" this by removing or loosening the 90 s window:** the timer is locked product law. **GAME-134 AC:** must require the produced SC-level table as an artefact. | A stated standard the locked behaviour cannot satisfy is unfalsifiable: GAME-134 could pass every listed check and still not be AA. Naming the two exceptions and the conforming untimed path makes the claim checkable, keeps the return contract intact, and matches the SC-level structure WCAG conformance actually requires. |
| CRIT-5 | "23-dimension scorecard" asserted in PRD §11.6 and TECH R-6 while PRD §6 contains 18 rows; no dimension enumerated; later citations ("rows 6 and 10") are meaningless | **ACCEPTED** | Publish **one canonical list of 23 dimensions with stable IDs** — the 18 existing rows become **Q-01…Q-18**, and the five rows the other accepted critiques require are added as **Q-19…Q-23** (Q-19 decorative-layer determinism/removability, Q-20 audio restraint + per-cue provenance, Q-21 earned-break return contract, Q-22 second-construction offer rate + engine-owned bonus, Q-23 authority-boundary enforcement). **PRD §6:** re-present the table with columns `ID | Dimension | "Meets" means | Evidence source | Threshold | Status | Ratifier`, using Q-IDs; state *"Q-01…Q-23 is the canonical set; GAME-172 scores exactly these IDs and no others."* **PRD §11.6 / §5 / Q2 / TECH R-6:** replace the words "scorecard rows 6 and 10" and "23-dimension scorecard" with `Q-06`/`Q-10` and "the 23 canonical dimensions Q-01…Q-23". **GAME-172 AC:** names Q-01…Q-23 explicitly. | The gate's evidence contract cannot be scored against a count the source table does not contain. Fixing the count by *defining the missing rows* (each one already demanded by an accepted critique) keeps the 23-dimension commitment while making every later citation unambiguous — cleaner than renumbering the claim down to 18. |
| CRIT-6 | The single locked policy-critical behaviour (a bounded window that must end) has split authority: engine owns "session facts", "host owns the clock", no monotonic/wall-clock definition, no hidden-tab-crossing rule, evidence is a DOM string scan | **ACCEPTED** | **One authority:** the engine owns the round deadline on a **monotonic** source (injected clock; `performance.now()`-based in production, fake-clock in test) and owns expiry; the host supplies **visibility events** and route lifecycle as engine *inputs* on a separate channel (this is not a new player intent and does not touch the closed intent union). **TECH §5.2:** delete `the engine owns session facts (90 s / 6 bridges) and the host owns the clock`; replace with the engine-deadline statement plus the explicit note that the intent union is unchanged. **PRD §4.1:** add `session deadline and expiry` to the engine's authority list. **PRD §3.8:** state the constants `ROUND_CAP_SECONDS = 90`, `ROUND_CAP_BRIDGES = 6` as engine constants; keep the single cap (O-3). **Semantics, written once:** `visibilitychange` → engine pauses or continues by surface (CRIT-7 table); on resume the engine compares monotonic elapsed against the remaining budget and, if the deadline passed while hidden, **expires deterministically** (break: save-at-expiry then return; free site: end the round and show summary honestly); device sleep is treated as hidden. **PRD §6 row Q-11 evidence:** delete `0 extension controls in DOM` as the evidence; replace with *"fake-clock + CDP page-hide e2e (deadline and expiry) + tamper case (no control, DOM mutation, query parameter or reload restores a window); DOM scan retained only as a secondary smoke check."* **View-model impact:** exposing the deadline in `BridgeViewModel` changes the frozen typed contract → recorded gap, vehicle **BB-CONTRACT-1** (bounded draft scope, §5 O-8). | A DOM scan cannot prove a round cannot be extended, and timer throttling on mobile Safari can silently extend or truncate the window — the exact failure the Pilot-0 trust gates exist to catch. One monotonic owner plus visibility inputs is the only testable arrangement, and the brief's authority rule already makes the engine the sole session authority. |
| CRIT-7 | Unbounded tab-blur pause plus a "cancellable" 3 s break return let a child avoid returning to practice indefinitely — the locked engagement failure | **ACCEPTED** | **Return-contract rule: it must be non-extendable.** **UX §2 cross-context rule:** replace `Tab-blur freezes the clock everywhere (…) no penalty` with: *"Tab-blur pauses the clock on the free site only, against a cumulative pause budget of 60 s per round; once the budget is exhausted the round ends at the next resume and the summary is shown honestly. Inside the earned break the window is **wall-clock**: blur does not stop the deadline (the pause copy says so: 'Paused — the break clock keeps running'), and an expired window returns the child to practice on resume."* **PRD §2 / §7:** replace "disclosed tab-blur pause with resume" with the two-surface rule. **UX §9 motion table:** replace `Break return | 3 s auto-return | Cancellable by one tap` with `Break return | 3 s auto-return, **non-cancellable and non-deferrable** | The visible 'Back to practice' button only returns **early**; it cannot cancel, postpone or extend.` **UX §2 J2.4 and PRD §2/§3.10:** keep "no extension affordance exists anywhere" as the governing statement; state that no copy, control or state may offer more time. **PRD §3 loop** must branch: free site → summary (Play again / Change level / All games); earned break → S4 return only (no summary, no Play again) — aligning PRD §3 with UX §6. **GAME-135 AC:** fake-clock + page-hide test for the pause budget and for wall-clock expiry in break; a tamper test that no extension path exists; `break_flow{…,pausedMs,endedReason,returnedToPractice}` in the canonical event table. **GAME-171 AC:** S4/S5 frames with the non-cancellable return and the disclosed break-clock copy. | Two mechanisms (unbounded blur pause; cancellable auto-return) defeat the only purpose of the earned break — protecting lesson time — on the surface the locked constraint exists to protect. Making the break wall-clock and the return non-cancellable restores the promise without touching the child's freedom on the free site. |
| CRIT-8 | Host-shell/return-contract surface is unowned (UX GAP-1), owned twice (UX §11.9 + GAME-135), and denied as a gap by the other two documents | **ACCEPTED** | **Single owner, by extending an existing child:** GAME-171's AC gains the host-surface clause (exact text in §6.2); the NG-09b draft story in UX GAP-1 is **withdrawn**; GAME-135 keeps only *runtime* ownership (interface contract, parity, fault/containment tests) and owns no design; GAME-133 is explicitly **not** the owner of shell surfaces. **UX §12 GAP-1:** replace the draft story and the "preferred alternative" with the recorded decision: *"Resolved: exactly one child owns production design of the host surfaces — GAME-171, by an extended AC. GAME-135 owns the runtime contract and enforcement tests. No new story (NG-09b withdrawn)."* **UX §11.9:** keep the surfaces in GAME-171's Figma scope and state that this is now the sole assignment. **PRD coverage check** and **TECH coverage check:** replace the "no gaps" verdicts with the ownership map + AC-edit list (CRIT-3). | Rule 4/3: prefer making the host-shell/return-contract surface owned by extending GAME-171's AC rather than creating a parallel story. GAME-171 already lists these surfaces in its Figma scope, so the extension is additive and removes both the duplicate and the false "no gaps" verdict. The *design* deadline ("before GAME-133 closes") is recorded in GAME-105's text as a GAME-171/133 sequencing condition. |
| CRIT-9 | The highest-severity technical risk (R-1) and the central evidence lane (real Phaser render) are mitigated only by work no child owns; GAME-135 "must not close until enforcement lands" appears only in a risk row | **ACCEPTED** | Name the vehicles, put the enforcement in ACs and DoD, not in prose. **GAME-135 AC:** add the **import-boundary test** (no engine/session/exactness module may import Phaser, layout or `unitPx`) and the decorative-layer cannot-emit-intents assertion; move "must not close until enforcement lands" from TECH §13 R-3 into GAME-135's **DoD**. **GAME-166:** extend its AC with the real-render lane (Playwright chromium `--use-gl=swiftshader`) and reopen it (it was heading to Done with the jsdom limitation as its deliverable); the vitest canvas-mock path is marked **non-evidence**. **PRD §11.10 / cover sheet:** relabel GAME-166 as *reopened / AC extended*, and mark the WebGL software-rendering lane `[required work]` with a named vehicle. **TECH §10.8/§10.9:** keep the text but add the owning story and AC per sentence. | Gate F is defined to depend on enforcement that no child was scoped to deliver, so it would either block forever or be waived by improvisation. The two edits add no new scope: they attach existing recommended work to the children that already own the surrounding responsibility. |
| CRIT-10 | `submit` / `incorrectSubmit` exist in the frozen contract, the 20 Figma state names and the UX keyboard list, but not in the PRD loop; either they are dead or the PRD loop is wrong | **ACCEPTED** | `submit` is **first-class** (it is already a frozen union member and `incorrectSubmit` is one of the 20 frozen state names mapped 1:1 to GAME-130 frames — deprecating it would change the frozen contract, so that branch is not available). **PRD §3 loop:** add the step after the placement branches: *"any time — including a complete-looking composition — the child may **Check it** (`submit`); a non-exact submit restates the difference as lengths (`incorrectSubmit`) and never reads as a verdict on the child; an exact composition auto-verdicts on placement."* **PRD §3.3:** add `submit` as the ninth listed action path (the union stays eight members) with the pointer/tap/keyboard route. **PRD §3.5:** state the `incorrectSubmit` copy rule (exact signed difference, no X, no buzzer, **0 penalties**). **PRD §3.11/UX §4 S1 + §5.3:** add the visible **Check it** control to the S1 inventory and to the keyboard map (focusable control, activated with Enter/Space; Esc still pauses). **PRD §9 telemetry:** add `submit_result{result, diff}` to the one canonical event table (MAJ-19). **Scorecard:** `incorrectSubmit` feedback is scored under `Q-05` (feedback clarity) and `Q-07` (recovery/undo) — no new row. **UX §3 severity row:** keep, now mapped to `Q-05`/`Q-07`. | The frozen contract and the 1:1 Figma mapping make `submit` part of the product; what was missing was the PRD's loop, HUD and telemetry. Making it first-class is the only lock-compatible resolution and it costs one visible control. |
| CRIT-11 | Comparator set defined three ways; no comparator maps to a scored threshold; one document states the demotion as decided while the same document asks it as an open question | **ACCEPTED** | **One registry table, one status per entry, one dimension mapping.** Statuses: **Railway Hero — scored** (primary educational/accessibility bar; its nine-criterion record is labelled *current-generation only* and is never cited as Phaser evidence); **Poly Bridge 3 — scored, tightly scoped** to construction-interaction dimensions only (`Q-06`, `Q-10`), never correctness, pacing or level length; **World of Goo 2 — registry-only (demoted)**, decision taken not recommended (O-1 confirms); **Bridge Constructor — folded** into the Poly Bridge profile (registry row, not scored); **Kahoot! Numbers by DragonBox — scored on `Q-04` only** (re-justified: zero-text onboarding/discoverability; its combine/split justification is deleted with CRIT-2); **Sushi Monster — scored on `Q-11` only** (progression legibility); **Hungry Fish/Guppy — registry-only**. **PRD §5:** replace the four prose decisions with the table (entry, status, dimensions benchmarked, what "Meets" and "Exceeds" mean against it, dated observation, access/licence check) and delete the "recommendation, Q2" wording; the PRD must not present a decision as taken and as open in the same document. **PRD §6:** every scored comparator must appear in the `Evidence source` of at least one Q-ID row; an entry that maps to no scored dimension is registry-only, not scored. **TECH coverage check:** replace `(Railway Hero / Poly Bridge 3 / World of Goo 2)` with "the comparator registry table in GAME-105 text". **GAME-172 AC:** scores against the registry table and records the access/licence check; loss of access becomes a recorded Gate E limitation, and no comparator asset, name, layout or audio may be reused (locked constraint, `Q-17`). | Gate E's scoring basis cannot change with which document the implementer reads, and a comparator that maps to no threshold is decoration. One table with explicit per-entry status and dimension mapping makes the demotion traceable and the bar operational. |
| CRIT-12 | Two documents define two different accessibility standards (2.1 AA vs 2.2 AA) for the same build; the PRD files the reconciliation as minor | **ACCEPTED** | **Standardise on WCAG 2.2 AA**, carried by the per-SC conformance statement from CRIT-4. **UX §7 first line:** replace `Target: WCAG 2.1 Level AA` with `Target: WCAG 2.2 AA, asserted per success criterion (see the conformance table; SC 2.2.1 and 2.2.2 not met, essential-limit exception claimed)`. **PRD §15 Q12:** promote from *minor* to a resolved contract decision and cite the standard + exceptions; **UX §12:** add the corresponding closed entry. **GAME-134 AC:** name the standard and the added-2.2 criteria that change the evidence set (2.5.8 Target Size (Minimum) exceeded by the 48 px floor; 2.4.11 Focus Not Obscured; 3.2.6 Consistent Help; 2.4.13 Focus Appearance) with one evidence line each. **Epic text:** one standard, one statement, one signer. | The accessibility gate cannot be scored against two criteria sets; whichever the implementer follows, one document's claim is false. 2.2 AA is the forward-compatible choice and the repo's 48 px floor already exceeds 2.5.8. |

---

## 2. Resolved Major Issues

| ID | Issue (short) | Disposition | Resolution + exact instruction a writer must apply | Rationale |
|---|---|---|---|---|
| MAJ-1 | Four of five free-site success metrics are unmeasurable (returning visitors, share and waitlist conversion, day-2 return) and one has no designed surface | **ACCEPTED** | **PRD §10, free-site bullet:** replace `'/games' start→summary completion ≥60 %; returning visitors; share and waitlist conversion attributable to the games surface` with *"'/games' start→summary completion rate ≥60 % (aggregate, session-scoped, no identifiers). Cross-visit metrics — returning visitors, day-2 voluntary return, share and waitlist conversion — are **not computable** under the session-scoped, no-PII posture and are **not claimed** until family accounts ship (non-goal here). No share or waitlist surface is designed in v1; share-to-social on the play surface remains a non-goal."* **PRD §10, in-app bullet:** delete `day-2 voluntary return`. **PRD §9:** name the two events that prove the retained metric (`session_start{mode:free}` → `session_end` with a summary-reached flag) and note the owner is GAME-135's telemetry AC. | A gate cannot be evaluated against metrics the privacy posture makes impossible; keeping the one computable metric and stating the others as not-claimed removes the invitation to retro-fit reporting. |
| MAJ-2 | `Ⓟ` marker used inconsistently inside cells; two ratifiers with no row-to-owner split; thresholds presented as proposed in one place and settled in another; stray budget (`no long task >150 ms`) not in the scorecard | **ACCEPTED** | **PRD §6 preamble:** replace the `⟨p⟩` convention with *"Every row carries a `Status` (existing contract value — cited to source — or **proposed Ⓟ**) and a single `Ratifier` (`GAME-135` technical rows, `GAME-172` experiential rows, `⟨owner⟩` where a product decision is implied). A value with no citation is proposed; it is never presented as an existing contract value."* **PRD §6 table:** add the two columns; move every `⟨p⟩` out of the cell body (this resolves MIN-1). **One canonical budget table:** the per-device-class table in TECH §9.1 is the single source for performance numbers; PRD §9 and rows `Q-11`/`Q-14` reference it instead of restating values, and `no long task >150 ms` is moved **into** that table for all three classes rather than sitting loose in PRD §9. (`Q-14` is the row formerly numbered 14.) | Ratification ownership cannot be inferred from a marker placed inside a cell; a status column plus a named ratifier per row makes "Meets" evaluable and stops two documents from disagreeing about whether a number is decided. |
| MAJ-3 | Hint stuck threshold given three ways (spec 25 s, shipped 20 s/40 s relaxed, UX ">25 s or 3 failed placements") and the Relaxed multiplier is arithmetically wrong | **ACCEPTED** | **PRD §3.9:** replace `Hints appear only via the sparkle affordance or at the stuck threshold` with *"Hints appear only via the sparkle affordance or when the engine's stuck condition fires. The engine constant is the single authority: **20 s with no legal progress OR 3 failed placements, whichever first**; under Relaxed the time trigger is **40 s** and the failure trigger **6** Ⓟ. No other threshold may appear in any document."* **PRD §3.8:** replace `doubles the stuck threshold` with the explicit 20→40 s / 3→6 pair Ⓟ. **UX §3 edge-case row:** replace `Stuck (>25 s, or 3 failed placements)` with the engine constants and add a `Scorecard ID` cell = `Q-08`. **PRD §15 Q10:** close as resolved (re-rate it major→resolved, not minor); the values are engine constants and the failure trigger pair is `Ⓟ` pending ratification by the owner/design and GAME-134's e2e. **Q-08 threshold:** add *"0 hints before the engine threshold; thresholds are the constants above (no document may state a different value)."* **PRD §3.6 vs UX §3:** one behaviour for `Try one level easier?` — mid-session, shown at most **once per round**, never an automatic band change. | A locked UX value that gates hint behaviour was unresolved in three documents, and 25×2 ≠ 40 means one document was arithmetically false. Adopting the shipped engine constants with the UX's second trigger (marked proposed) gives e2e something to assert. |
| MAJ-4 | Locked 90 s / 6-bridge cap presented as settled in row 11 while Q4 reopens it and A3 records a 5-bridge note | **ACCEPTED** | **PRD §3.8 / §6 Q-11:** keep the single cap and cite the engine constants (`ROUND_CAP_SECONDS = 90`, `ROUND_CAP_BRIDGES = 6`, whichever first). **PRD §15:** delete Q4; state in §14 A3 that the 5-bridge prototype note is historical and superseded, and that changing the cap requires an owner decision that supersedes the locked brief **in writing** (O-3). **PRD §5:** comparator scoping must not assume band-variable round lengths. | A locked constraint may not be reopened by an open question in the same document that scores it as final; the only permissible route to band variance is an explicit written owner override, which is tabled as O-3. |
| MAJ-5 | Automated axe cannot evidence canvas, keyboard-operability, reduced-motion or mirror claims; the "six named facts" are never named; the manual AT pass and the device evidence have no executor | **ACCEPTED** | **PRD §8:** enumerate the mirror facts once — **F1** available pieces (label + units + denominator), **F2** selected piece, **F3** exact current composition, **F4** required span, **F5** remaining/over amount (signed), **F6** verdict, **F7** remaining time + pause state (added by MAJ-25) — and state that assertions are driven **from the DOM mirror**, never from the canvas. **Q-12 threshold:** replace `semantic mirror exposes six named facts` with `semantic mirror exposes F1–F7`; add reduced-motion, grayscale and keyboard-only assertions; state that `0 serious/critical axe` covers DOM surfaces only and is never cited as canvas evidence. **GAME-134 AC:** the manual VoiceOver/NVDA pass carries a **named executor and date** (O-7) or Gate D is formally recorded as blocked. | Automation that cannot inspect the surface it is cited for cannot fail on the properties that matter; naming the facts and driving assertions from the mirror makes "accessibility qualified" a claim that can be falsified. |
| MAJ-6 | "Learning happened measurably" is only schema-level: no baseline, no sample minimum, no round/session definition, and "complete rotation per round" is impossible (14 skills, 6 bridges) | **ACCEPTED** | **PRD §4.4:** define the objects — **round** = one bounded instance (90 s/6 bridges; Relaxed = 6 constructions) and **session** = one tab session containing ≥2 rounds or ≥12 puzzles — and give each signal a sample rule: hint-free solve rate and overhang→fit recovery rate are computed **per session with a minimum sample of 12 puzzles**; below that sample no trend is reported. Delete `expected to rise within a session`; replace with *"reported per session with the sample minimum; a within-session rise may be reported only as a descriptive comparison at n≥12, with no causal claim."* **Q-02 threshold:** replace `complete rotation per round` with `14/14 generatable and reachable; every round draws ≥6 distinct skills; no skill starves across 3 consecutive rounds; a complete 14-skill rotation is achievable within one session (≥12 puzzles)`. **Q-16 threshold:** *"hint-free, recovery and second-construction rates computable per session at the sample minimum; no efficacy claim without a study."* | The epic was shipping a "measurable" learning claim that could not be computed or falsified, and one row was arithmetically impossible. Round/session definitions plus sample minimums make GAME-172's evidence scoreable. |
| MAJ-7 | Magnet assist (≈12 px) means snap tolerance can select *which* placement occurs, so the authority claim is proven for the verdict function but not for the renderer→intent path | **ACCEPTED** | Reduce snapping to a **visual** effect: **PRD §3.3:** `pointer drag with magnet assist` → *"pointer drag with a purely visual magnet offset (≤12 px) that never changes the target slot; the target slot is the nearest engine-derived slot centre (see `Q-23`)"*. **TECH §6:** state the pointer→intent resolution rule (nearest slot centre from engine-derived slot geometry in units; tolerance constrained below half the minimum slot pixel pitch) and that it is a pure function of the layout, never of the verdict. **GAME-135 AC:** identical intent sequences under different `snapTolerancePx`, `unitPx` and DPR must yield **identical placements and identical legal-action sets** (this replaces the vacuous assertion in MIN-10). **Q-23 threshold:** includes this invariance. | Snapping distance is a locked forbidden correctness input; a magnet that can select a different slot is a legality input by another name. Making the assist visual-only and testing the real path closes the gap the fixtures could not see. |
| MAJ-8 | Decorative sag/landing thud is required by the PRD but its `seed` is absent from the view-model, and nothing prevents the decorative layer from becoming a second authority | **ACCEPTED** | **View-model:** add a deterministic `renderSeed` (puzzle-derived id) → recorded frozen-contract gap, vehicle **BB-CONTRACT-1** (O-8). **TECH §6:** keep `onCrossingStart(seed)` but source the seed from the view-model, and add: *"presentation modules may never emit intents; the intent channel has exactly one producer (`normalizeInput`)"*. **GAME-135 AC:** contract test that no presentation/decorative module can emit an intent, plus determinism ACs (identical resting frame at a fixed seed across frame rates, DPR 1/2/3 and reduced motion; `sag = 0` under reduced motion). **Scorecard:** new row **Q-19** — *"decorative layer removable and non-authoritative"* (meets: 0 decorative intents; identical final frame at fixed seed; sag collapses under reduced motion). **PRD §3.7:** keep the sag (perceived consequence) and add the determinism/removability qualifier. | Scoring inputs (`pieces`, `par`) feeding a decorative layer whose determinism condition the contract could not satisfy is precisely the contamination risk the brief names; the seed belongs in the authoritative channel and the one-way flow must be asserted. |
| MAJ-9 | Several "unmarked = existing contract value" thresholds have no citable contract (rows 3, 7, 8, 17) | **ACCEPTED** | Apply the MAJ-2 status rule: each of `Q-03` (≥45 % g12 multi-solution supply; ≥2 distinct builds), `Q-07` (0 penalties; unlimited retries), `Q-08` (≤1 ghosted valid placement; 0 full-solution exposures) and `Q-17` (0 comparator identifiers; per-asset provenance) is either **cited to an exact source** (file + section, ledger entry, contract clause or commit) or marked **`Ⓟ` with a named ratifier** — the writer chooses per value and may not leave it unmarked. Add to the epic text: *"no authored-but-unexecuted check and no uncited number may be described as existing."* | Provenance discipline is an explicit acceptance criterion of this run; labelling new thresholds as pre-existing removes the ratification they require and misrepresents their support. |
| MAJ-10 | All three documents reject a CI story on an unverified equivalence — but CONSULTING-305 scopes only the *current-generation* suites, while next-gen enforcement is what Gate F depends on | **ACCEPTED** | **GAME-105 epic text and GAME-135's DoD:** state that **CONSULTING-305 is extended** to the next-generation suites, with the explicit suite list — next-gen Playwright journeys, axe over the DOM mirror, the real-render swiftshader lane, bundle-size and frame-timing budgets, and the import-boundary test. **Do not create a second GAME story for CI** (locked: no duplicate ownership). **Timing** is O-4: recommendation is that the exactness/a11y/import-boundary suites enforce before Gate F and the real-render lane may land after Gate F under a dated owner waiver. | Three documents agreeing on an out-of-scope ticket does not create an owner for the suites that gate the cutover; extending the existing cross-project dependency keeps single ownership while naming the suites. |
| MAJ-11 | Educational goals disposed as "epic-text gap only", yet nothing owns per-band content-volume sufficiency, the decoy-honesty audit at content scale, or the stale-CCSS reconciliation; row 2 asserts 14/14 with no coverage object | **ACCEPTED** | **Epic text (PRD §4.2/§4.4 + GAME-105):** define the coverage object — *supply floor per skill × band × difficulty tier, ≥12 distinct solvable puzzles per cell Ⓟ* — and the decoy-honesty property as a named test (`decoyHonesty`: every decoy either keeps the puzzle solvable or overshoots by more than the largest plank). **GAME-129 fixture family:** reference the two properties (no new story needed for the tests themselves if they extend the existing generator property suite); **catalogue of record:** the CCSS edit (remove the stale conditional `3.MD.D.8` and the future `6.G.A.1`, reconcile to the shipped 14-skill catalogue) is a jira-admin/content edit, not product engineering. **New bounded issue proposed — BB-CONTENT-1** (subject: curriculum catalogue of record + content-volume supply floor; scope: catalogue edit, supply-floor property test, decoy-honesty test at content scale; AC: property tests green for all bands, catalogue matches `skills.ts`, no stale code); sanction is O-8. **Q-02 evidence:** cite the property test, not `skills.ts` alone. | Reachability of 14 skills is the product's core educational promise and was unfalsifiable without an owned content-volume definition; the bounded issue is the minimum that makes the claim testable and the rule-3 statement that no existing child carries it. |
| MAJ-12 | Audio has no home: "deliberately does not make the investment", a nice-to-have ambient bed, a required landing thud, and DQ-6's rejection conditional on an unowned commission | **ACCEPTED** (licensed/bespoke branch DEFERRED, §4) | **Epic text + PRD §12 + TECH §2.5:** publish the **cue inventory** (pickup/select, placement confirm, under/over, exact-fit, assembly/crossing, completion, break-return prompt) with a per-cue provenance rule: **original synthesized WebAudio is the final-state baseline**; any licensed or bespoke cue requires a provenance record (source, licence, no comparator reuse) and owner sign-off. Freeze as final state: **no bespoke or licensed music** (consistent with the Poly Bridge/Goo reasoning in §5). Ownership: runtime cues = **GAME-133**, mix ceiling + mute variants = **GAME-171**, audio-described parity = **GAME-134**. **PRD §15 Q7 and UX DQ-6:** close as decided (no separate audio story; no commissioned scoring in v1). **Scorecard:** new row **Q-20** — *"audio restraint and provenance"* (meets: no alarm/ticking transient; mute preserves all meaning; per-cue provenance complete). | BRIEF §4 requires a technology review with responsibility per technology including audio, and comparator audio reuse is forbidden; freezing the synthesized cue set with a published inventory gives the requirement an owner without inventing a story or a licensing surface. |
| MAJ-13 | The document's only stated blocker (no named human/date for four human gates) is filed as an unaddressed open question and omitted from the action list; TECH rates the same family (claims drift) as minor | **ACCEPTED** | **PRD §15 Q1:** convert to an owner-decision request (**O-7**) with a required owner and target date; record it in **GAME-105's DoD** as a closure condition (Gates D and E cannot close without it). **PRD §16 / action list:** add the owner ask to the action list. **TECH §13 R-9:** re-rate **MINOR → MAJOR** (see MIN-20) and add the two missing risk rows (bounded-session breach with CRIT-6/7 mitigations; dual-renderer divergence with the MAJ-14/16 mitigations), each with an owner. | A deliverable that declares itself the source of truth while deferring its own gate-closing condition cannot be consumed as one, and claims drift is a locked-constraint risk, not a nit. |
| MAJ-14 | The React DOM renderer that stays in production until Gate F has no budget, no owner and no parity criteria; hub/lesson integration and funnel instrumentation have no owner | **ACCEPTED** (funnel-instrumentation branch DEFERRED, §4) | **TECH §9.1:** add a **DOM-first / React-path row set** with explicit budgets for class A/B/C (first interactive as specified, input→feedback ≤100 ms phone/desktop, memory ≤128 MB class C, DOM bundle delta budget Ⓟ) and a statement that the DOM path is a **production renderer during the dual-renderer period**, not only a fallback. **TECH §11.2:** add the parity criteria list (exactness, accessibility, telemetry/privacy, lifecycle, performance) that must be recorded before DOM retirement — owned by **GAME-135**, evidenced by a parity suite. **Owners:** hub/lesson integration = GAME-135 (`React` session/lifecycle AC) with GAME-171 owning its design; documentation of the dual-renderer period = GAME-143. **PRD §9:** state the React path's closure evidence rather than leaving it implicit. | The dual-renderer period *is* the release plan; with only one renderer carrying a quality bar, "parity" at Gate F cannot be asserted. Adding the row costs a table entry and closes the largest unowned area of the release plan. |
| MAJ-15 | Neither view-model nor engine algorithm expresses placement geometry, yet the PRD requires order-dependent fill, per-plank overhang anchoring, geometrically distinct multiple solutions and a `constructionVector` | **ACCEPTED** | **View-model (via BB-CONTRACT-1):** extend with an **ordered, unit-based** placement representation — `slots[{slotIndex, offsetUnits, pieceId?}]`, `openSlots[]`, `fillOrder[]` — never pixels; keep `placedComposition{units,label}` as the aggregate. **Engine:** fill order and slot occupancy become engine outputs (the renderer may not invent them). **Definition to write once:** *"two solutions are geometrically distinct iff their multiset-of-interval decompositions (piece lengths and slot offsets, in units) differ."* **PRD §4.2 / Q-03 evidence:** cite the unit-level distinctness definition. **Q-23:** add "no renderer-derived geometry in the verdict or legality path". | With only an aggregate sum, "geometrically distinct" cannot be evaluated and the renderer necessarily invents authoritative-looking geometry — the boundary risk decided in the layer the design declares non-authoritative. |
| MAJ-16 | The DOM mirror is specified as a second full renderer with automatic fallback, but has no DOM-first budget, no suite, no detection latency and no failover UX | **ACCEPTED-MODIFIED** (the "drop the DOM-first claim" branch is **REJECTED** — see §5) | Keep full-round DOM-first capability (the locked accessibility requirement makes the non-canvas path mandatory) and make it verifiable. **TECH §3.7/§7:** state the **degraded mode** (DOM-first play: semantic controls + labels + mirror facts F1–F7; one announcement on entry; no canvas), detection latency ≤1 s, focus restoration rule (focus moves to the mirror heading, then to the last focused control id if it still exists, otherwise the first tray control), and failover UX (in-place panel; the round **continues**, it does not restart). **TECH §9.1 / §10:** add the DOM-first budget row and a DOM-first a11y/exactness suite. **GAME-135 AC:** mid-round failover fault test (renderer crash, WebGL context loss, dynamic-chunk failure, asset 404) — and only with that test may the epic keep the claim *"a child never loses the round"*. | The gap is real (an unverified fallback shipping under a safety claim), but the prescribed alternative of demoting the mirror to presentation-only would breach the locked requirement that the game be fully operable and understandable without the canvas. Adding budget, suite and failover UX keeps the capability and makes it provable. |
| MAJ-17 | Contract versioning says unknown/malformed input fails closed, while a percentage rollout permanently retains two renderers — no compatibility matrix, no skew detection, no defined behaviour for dropped input | **ACCEPTED** | **TECH §5.3:** publish the policy — host pins the renderer bundle per session; the renderer refuses to mount on a version mismatch and the host pins `dom` for that session; **supported compatibility is same-version or same-major +1 additive**; a major mismatch fails closed to `dom`. **User-visible behaviour:** a dropped intent is **not** silent — the input is ignored and a non-blocking retry affordance appears after 500 ms ("Tap again"), with `stale_intent_dropped` telemetry; `renderer_version_skew` is added to the event table. **GAME-135 AC:** version-skew fault test and the retry-affordance assertion. | Any version skew during a staged rollout otherwise produces silently dropped input or a frozen scene, which is indistinguishable from a broken game to a child; a stated compat matrix plus visible retry makes skew observable and safe. |
| MAJ-18 | `unitPx` derived from the viewport contradicts row 9's absolute 24 px threshold; PRD claims DPR to 3 while TECH caps at 2; no hit-area overlap rule; no rule for an exactly-filled span wider than a 200 %-zoom viewport | **ACCEPTED** | **Q-09 threshold:** restate as a **proportionality invariant + legibility floor** — `widthPx = units × unitPx` exactly (≤1 px rounding error), one-unit g12 plank **≥24 px and ≤48 px** on the smallest supported viewport, and when the composed span exceeds the viewport width the bridge **pans as a unit; it never scales below the floor**. **PRD §9 / §3.11:** state the clamp, the pan-vs-scale rule for narrow viewports and the 200 %-zoom behaviour (only the bridge pans, as one unit). **DPR:** single policy — device DPR ≤3 is supported; the internal render scale is capped at 2 for fill cost; visual parity is asserted at DPR 1/2/3. **TECH §3.6:** define hit-area expansion caps (≤±12 px beyond the visual bounds) with a **nearest-centre priority rule** (deterministic tie-break by lower `slotIndex`) and the ≥8 px visual spacing rule. | Row 9 is the core pedagogy dimension and was unmeasurable at the stated minimum viewport; the DPR claims could not both be true. A proportionality invariant plus explicit clamp, cap and hit-priority rules makes the row evaluable at every viewport. |
| MAJ-19 | Two divergent telemetry contracts (enums differ, PRD omits `overshoot` and the lifecycle events); no sink, retention, access control or alert owner/threshold | **ACCEPTED** | **One canonical event table in the epic text**, referenced by PRD §9 and TECH §11.1 with no restatement: `session_start{mode,band,source}`, `puzzle_start{skillId,puzzleId,gapValue,trayHash}`, `place_attempt{result: fit\|partial\|overhang\|overshoot, diff, pieceValue, msSincePuzzleStart}`, `submit_result{result,diff}`, `hint_shown{level,puzzleId}`, `puzzle_solved{piecesUsed,par,solveMs,hintLevelMax,constructionVector}`, `second_build{offered,accepted,solved}`, `streak_update{current,best}`, `session_end{bridgesBuilt,score,bestStreak,avgHints,coachingId,durationMs,summaryReached}`, `break_flow{unlocked,started,endedReason,pausedMs,returnedToPractice}`, lifecycle `renderer_init`, `renderer_init_failure`, `renderer_remount`, `stale_intent_dropped`, `renderer_version_skew`, and `invariant_violation{where}`. **Sink/retention/access:** the existing LevelBest telemetry sink used by current-generation work (cite the ledger entry when the epic is written); client-side session scope; server-side **aggregates only, 30-day raw retention Ⓟ**; access = owner + engineering, no third parties. **Alerts:** `invariant_violation` **>0 in any 24 h → owner alert (P0)**; `renderer_init_failure` **>2 % of sessions in 1 h → alert + automatic pin to `dom`**; thresholds/owner/on-call recorded in the epic. **PRD §15 Q11:** closed. **Defect taxonomy (MIN-7):** P0 authority or privacy leak, P1 dropped input / broken round, P2 copy or state violation, P3 polish. | The epic must carry one canonical event table — divergent enums break schema validation and an alert with no threshold, owner or sink cannot be an operational control for a locked constraint. |
| MAJ-20 | The entire host-shell interface is one sentence: no props/events, no expiry save/return handshake, no failure behaviour, no lesson-shell resume contract | **ACCEPTED** | **TECH §5.2:** publish the contract — `GameHostProps{mode:'free'\|'break', band?, timeLimitMs, pauseBudgetMs, onExpire, onReturn, seatContext?}`; `GameHostEvents{onSessionStart, onRoundEnd, onBreakExpired{saveId}, onReturnedToPractice, onExit}`; expiry handshake (engine freezes the composition → host persists the session-scoped save → host shows S4 → return → `break_flow{returnedToPractice:true}`); failure behaviour (break host unavailable → fail closed to practice, never to free play); teacher/parent-launched entry (unlock card skipped, band supplied, same window). **Ownership:** runtime interface + parity/fault tests = **GAME-135**; design of the host surfaces = **GAME-171** (extended AC); **GAME-133 is not an owner of shell surfaces**. **GAME-105 text** references the contract. | The locked return promise was unimplementable and untestable from either side of the boundary, and this is the same surface CRIT-8 shows to be unowned; publishing the interface closes both. |
| MAJ-21 | The break countdown is specified three inconsistent ways and the one thing that changes at 10 s is specified in colour only | **ACCEPTED** | **One state sequence, written once (PRD §2, UX J2 §2, UX §9):** ring visible from second 1, steady warm amber, **never pulsing/flashing/ticking with sound**; at ≤10 s the state changes through **non-colour carriers** — the ring becomes **segmented** (one segment drops per second), the numeric remaining time becomes **visible text**, and the copy switches to "Back to practice soon"; colour stays steady amber. **UX §9 pacing rule:** replace `the final 10 s … changes state (steady ring)` with the full sequence above. **PRD §2:** add the segment + numeric carriers. **Q-11/Q-21:** assert "0 colour-only state changes; ring state sequence present". | The break's only time affordance was specified three ways and the only state change was colour-only, which violates the product's own non-colour rule and is exactly what GAME-134 must qualify. |
| MAJ-22 | Second-construction card (5 s auto-dismiss) versus the 1.5 s next-puzzle pacing rule and the 3 s break return | **ACCEPTED-MODIFIED** (the blocking/non-blocking dichotomy is a false one — see §5) | Define it as a **non-blocking inline offer**: the next puzzle slides in after 900–1500 ms and is immediately playable; the card is anchored beside the HUD, is never modal, its 5 s auto-dismiss is its **lifetime, not a gate**, and it is never re-offered on that puzzle; **no card is offered at round end or inside the earned break**. **Q-10 threshold:** restate as *"next puzzle interactable ≤1.5 s on 100 % of transitions (the offer is non-blocking); crossing ≤700 ms; second-construction offer ≤40 % of solvable puzzles, decline neutral, 0 re-offers."* **UX §9 timing table:** rewrite the card row accordingly. | Either reading of the v1 text broke something; a non-blocking inline card with a lifetime satisfies both the pacing rule and the offer, and the 5 s number gains an observable meaning. |
| MAJ-23 | UX proposes deferring a mode the PRD presents as shipped, on a deadline ("before GAME-133 closes") that appears nowhere else | **ACCEPTED** | **PRD §3.8/§9/§12:** remove every claim that a free-build shelf is shipped (see CRIT-1); the mode is an **unshipped candidate (G-05)** unless the repository/ledger proves otherwise, and it is a v1 **non-goal**. **Decision deadline:** record in GAME-105's text and in GAME-133's DoD: *"any design or asset work for a free-build shelf is out of scope; GAME-133 does not wait on it. If the owner later reverses the non-goal, it is a new bounded story and must be decided before GAME-133 closes to avoid art re-work."* **Q-11/Q-21:** the memory budget is restated on the supported round shape (MIN-4). | Regression-by-omission was the risk; stating the mode's status, the non-goal and the deadline in the epic removes the last artefact where the decision could be lost. |
| MAJ-24 | UX art direction forbids characters; PRD requires a crossing **critter** and TECH builds "critter-weight sag" | **ACCEPTED** | **Decide once:** the crossing subject is an **abstract non-character load marker** (e.g. a lantern cart / suspended crate). **PRD §3.7:** replace `a crossing critter traverses` with *"an abstract non-character load marker traverses the completed span (decorative, post-verdict, non-authoritative)"*, and replace `critter-weight` with `marker load`. **TECH §1.1/§6:** rename the decorative function to `onCrossingStart(seed)` with marker-load sag derived from inefficiency (`pieces − par`). **UX §11.1 + Figma requirements:** keep "no characters, no railway/train/villain motifs" and add the marker to the asset list with an **IP provenance requirement** (original asset, provenance manifest entry, no comparator derivation). | A character asset is forbidden by the product's own art direction and is the highest IP-expression surface the brief restricts; an abstract marker satisfies both documents and keeps the art system flat and vector-based. |
| MAJ-25 | Remaining time is absent from the semantic mirror, so a screen-reader user in the break cannot perceive the return window | **ACCEPTED** | Add **F7 (remaining time + pause state)** to the mirror list in **PRD §8** and **UX §4/§7**. **Announcement cadence (avoid chatter):** polite announcement at 60 s, 30 s and 10 s remaining and on pause/resume; the numeric value is always present in the mirror text; **assertive** remains reserved for the break-return prompt. **Q-12:** assert F7 and the cadence. **GAME-134 automation:** must assert the announcements. | The parity claim was false for the exact surface where time is policy-critical; adding the fact with a defined cadence fixes the claim without making the live region chatty. |
| MAJ-26 | UX severity ratings are consumed by no story or scorecard row, and PRD rows 4/5/7 name evidence no UX artifact defines | **ACCEPTED** | **UX §3:** add a `Scorecard ID / owning child` column; each BLOCKER/MAJOR row maps to a `Q-` ID (e.g. overfill → `Q-05`/`Q-07`; tray-exhausted → `Q-03`/`Q-18`; 200 % zoom clipped → `Q-13`; renderer init failure → `Q-18`; slow load → `Q-14`; route exit/interrupted crossing → `Q-18` + GAME-135 fault test). Keep only the "severity if wrong" labels as local UX severity; they are **not** gating severities. **PRD §6 rows 4/5/7 evidence column:** name the artefact and owner — child playtest notes = **GAME-172** `[required work]`; Playwright journey = **GAME-135**; UX state matrix = **GAME-171** appendix. | The UX's own severity set and the scored set disagreed about what Gate E must prove, so edge cases rated BLOCKER could ship untested; one mapped list makes every claim traceable to a dimension and an owner. |

---

## 3. Resolved Minor Issues

| ID | Issue (short) | Disposition | Resolution + exact instruction a writer must apply | Rationale |
|---|---|---|---|---|
| MIN-1 | `Ⓟ` marker placed inconsistently inside cells | **ACCEPTED** | Subsumed by MAJ-2: **PRD §6** gains `Status` and `Ratifier` columns; every `⟨p⟩`/`Ⓟ` is removed from cell bodies. | Ambiguity about *what* is proposed propagates directly into AC wording and ratification. |
| MIN-2 | Row 11 evidence ("clock + fake-clock e2e") cannot exercise `visibilitychange` | **ACCEPTED** | Subsumed by CRIT-6/CRIT-7: **Q-11 evidence** becomes fake-clock + CDP-driven page-hide + resume + tamper; the blur semantics move out of prose into the single written rule in PRD §2/§7 and UX §2. | A stated behaviour with no executable check is not a requirement. |
| MIN-3 | "≥50 fps p95" is not a well-formed statistic; PRD and TECH budgets disagree | **ACCEPTED** | Adopt **TECH §9.1's per-device-class table as the single canonical budget set**; express frame cost as interval percentiles (p95 frame time) **and** % of frames meeting the threshold; delete the PRD's duplicate numbers and the p75 mixed-percentile row; sample basis and device class stated per row. `Q-14` and `Q-11` reference the canonical table. | Two documents cannot both be right, and a percentile without a sample basis or device class cannot be measured. |
| MIN-4 | 20-bridge sandbox memory budget has no protocol and depends on the unowned mode | **REJECTED** (prescribed remedy) | The observation is right, the remedy ("drop the row until the mode is owned") is not: it deletes a genuine resource-leak guard. **Instruction instead:** **PRD §9/Q-14** replace `≤15 % memory growth across a 20-bridge sandbox session` with *"≤15 % heap growth across 2 consecutive bounded rounds; protocol = CDP heap snapshot before the first and after the second round with a forced GC; zero leaked listeners/timers after teardown (lifecycle test)."* | Rejecting the remedy, not the finding: an unbounded-mode measurement was unusable, but the leak requirement is real and must survive on the supported round shape. |
| MIN-5 | No scorecard row covers audio-cue restraint or the earned-break return contract | **ACCEPTED** | Added as **Q-20** (audio restraint + per-cue provenance; MAJ-12) and **Q-21** (earned-break return contract: save-at-expiry, non-cancellable 3 s return, 0 extension affordances, disclosed break-clock pause; CRIT-6/CRIT-7). Both are gated rows, not polish. | Unscored locked constraints are the first thing dropped during remediation. |
| MIN-6 | `[existing]` tags asserted without citing the ledger entry/test/commit | **ACCEPTED** | **PRD §2, §7 and every `[existing]` occurrence:** each tag carries its source (`SPRINT_READINESS.md` §, `UI_TEST_FINDINGS.md` entry, test name or commit/PR). Add the epic-text rule: *"an `[existing]` tag without a citation is treated as `[required work]`."* | In a provenance-discipline run, an uncited tag is indistinguishable from an authored claim. |
| MIN-7 | "zero P0/P1 defects" with no severity taxonomy | **ACCEPTED** | Define in the epic text (see MAJ-19): **P0** = authority leak, privacy/PII leak, or shipped incorrect-fit verdict; **P1** = dropped input / unrecoverable round / broken return contract; **P2** = copy, non-colour or state-contract violation; **P3** = visual polish. `Q-18` threshold uses P0/P1. | A gating metric with undefined severity levels is unfalsifiable. |
| MIN-8 | IP separation evidenced only by a null string scan | **ACCEPTED** | **Q-17 evidence** becomes *"0 comparator identifiers in shipped source **and** a per-asset provenance manifest (asset → source → licence → hash) reviewed at Gate G"*; the manifest is owned by GAME-143 with design input from GAME-171. | A string scan passes trivially on renamed-but-derived art or audio. |
| MIN-9 | `pieceId?` optional for all intents; only malformed payloads are defined | **ACCEPTED** | Define required fields per intent type and their rejection telemetry (`invalid_intent_rejected`): `selectPiece/placePiece/removePiece` require `pieceId`; `reset/submit/continue` require none; `requestHint` optional `level`; all require `seq`, `sessionId`, `generation`. Recorded as a **frozen-contract gap → BB-CONTRACT-1** (O-8); until it lands, the engine must treat a missing required field as an unknown intent (fail closed). | The channel that carries all player authority had unspecified validation. |
| MIN-10 | The property test asserts invariance of a function the engine cannot see layout for — it proves the signature, not the boundary | **ACCEPTED** | Replace with the real-path test (MAJ-7): identical intent sequences under varying `snapTolerancePx`, `unitPx`, DPR and hit-layer geometry yield identical placements and legal-action sets. Keep the pure-function assertion only as a cheap extra. **TECH §10.1** reworded accordingly. | A test that cannot fail for the real risk creates false confidence in R-1. |
| MIN-11 | Path (c) "vitest alias-level canvas mock with a real Phaser import" contradicts the requirement that the test must fail when rendering breaks | **ACCEPTED** | **TECH §10.3:** mark path (c) explicitly **"non-evidence — must not be cited in any gate packet"** and keep only (a) swiftshader Playwright and (b) node-canvas as viable lanes. | A fallback advertised as viable invites a green-but-worthless test. |
| MIN-12 | Alerting stated without thresholds, owners, on-call or severity mapping | **ACCEPTED** | Subsumed by MAJ-19: thresholds (`invariant_violation` >0/24 h → P0 owner alert; `renderer_init_failure` >2 %/1 h → alert + auto-pin `dom`), owner, escalation and P0–P3 mapping are recorded in the epic text; **TECH §7** error list gains the severity column. | Operationally unenforceable controls for the two states that indicate the authority boundary or the renderer has failed. |
| MIN-13 | 1:1 mapping between 20 view-model state names and "a named Figma frame" asserted although production frames are required work | **ACCEPTED** | **TECH §3.1:** restate as *"each of the 20 state names must map 1:1 to a named production frame; this mapping is a **GAME-171 acceptance criterion (`[required work]`)**, verified against the Figma file, not an established fact."* | Provenance-adjacent claim that the run's rules require to be marked as required work. |
| MIN-14 | No CSP, asset-integrity or third-party-script policy for a child-facing canvas site | **ACCEPTED** | **TECH §8:** add the content-security posture — CSP `script-src 'self'` (no third-party scripts, no inline eval), `frame-ancestors` restricted, SRI/hash verification for every CDN-delivered asset, asset integrity hashes recorded in the build manifest, no third-party analytics on the play surface. | Child-safe defaults need a technical expression, not just an intent. |
| MIN-15 | Verdict vocabulary differs across three documents (`overfill` vs `overhang`/`overshoot`; telemetry omits `overshoot`) | **ACCEPTED** | Publish one canonical mapping table in the epic text — `engine verdict → presentation state name → telemetry result value`: `fit → exact → fit`; `partial → underfill/remainingSpan → partial`; `overhang → overfill → overhang`; `overshoot → overfill (signed excess shown) → overshoot`; `invalidSubmit → incorrectSubmit → invalid_submit`. **PRD §9**, **TECH §4**, and **UX §5/§11.3** reference the table and stop restating enums. | Schema validation and Figma annotation both break if the vocabulary drifts a third time. |
| MIN-16 | `dragging`/`placementPreview` defined with pointer-only visuals, violating the section's own rule and the "0 drag-only actions" row | **ACCEPTED** | **UX §5 keyboard row:** define the keyboard/tap equivalents — slot focus renders the same ghost preview with the projected remaining span; keyboard picks a piece then a slot (`placementPreview` appears on slot focus); pointer drag remains a visual shortcut, never the only path. `Q-06` asserts it. | The parity assertion and the state inventory contradicted each other, weakening a gated accessibility row. |
| MIN-17 | DQ entries (DQ-2/4/6) carry recommendations but no addressee, date or severity; DQ-6's rejection is conditional on an unowned future | **ACCEPTED** (DQ-4 default deferred, §4) | **UX §12:** add `Owner`, `Date`, `Severity` columns to every DQ; close **DQ-6** as decided by the audio cue-set freeze (MAJ-12) with no residual conditional; close **DQ-3** as the CRIT-1/MAJ-23 non-goal; keep **DQ-4** open with a named owner and date. | Owner decisions without owners cannot be closed, and an unowned conditional re-opens a story that was already rejected. |
| MIN-18 | Blender determination assigned to three different owners/gates | **ACCEPTED** | Single decision owner: the **product owner** (O-5); evidence for any flip supplied by **GAME-172** (a material `Below` on world cohesion); the durable **ADR recorded by GAME-143**. **PRD coverage row:** replace `decision point stays GAME-171` with `decision owner = owner (O-5); flip evidence = GAME-172; ADR = GAME-143`. | A technology determination with three owners cannot be closed or traced; three artefacts each doing one job is single ownership, not three. |
| MIN-19 | Two names — "Workshop" and "Sandbox" — for what appears to be one mode, with no mapping | **ACCEPTED** | Both names retired (CRIT-1): the v1 unclocked mode is **Relaxed build**; the free-build shelf is an explicit non-goal and is written as *"previously referred to as Workshop / Sandbox — retired names"* once, in the epic text, so historical references remain traceable. | The identity of the mode decides what GAP-2 even is; one name plus a mapping note prevents a phantom requirement. |
| MIN-20 | R-9 (claims drift) rated MINOR in a no-fabrication run; the two largest product risks absent from the risk table | **ACCEPTED** | **TECH §13:** re-rate **R-9 MINOR → MAJOR** and add **R-11 bounded-session breach** (blur/pause/extension path; owner GAME-135; mitigations CRIT-6/CRIT-7 + the non-extendability test) and **R-12 retained-DOM divergence** (owner GAME-135; mitigations MAJ-14/MAJ-16 budget + parity suite). Each row gets an owner column. | Risk severity drives attention; the run's core integrity risk was minimised while the two risks the documents actually contain were missing. |
| MIN-21 | The second-construction bonus (+5) exists only in the UX flow, and the offer rate has no owner or measurement | **REJECTED** (scorecard-row part) | The relocation is accepted: **the `+5` and the offer rule move into the engine scoring table in the epic text** (the engine owns scoring — PRD §4.1), and the offer rate is proved by `second_build{offered,accepted,solved}` telemetry asserted under **Q-10**. The remedy's second half — *"add a row for the offer rate"* — is **rejected**: the scorecard is fixed at exactly `Q-01…Q-23` (CRIT-5) and `Q-10`/`Q-22` already carry the offer-rate and bonus assertions. | Adding a 24th row would break the canonical set GAME-172 must score, and a scoring constant outside the scoring authority is fixed by moving it, not by adding a dimension. |

---

## 4. Deferred Items

These are **partial deferrals** — no critique item was deferred wholesale (see §0). Each row states the disposition of the parent item, the exact scope that is deferred, and the condition that brings it back.

| ID (parent) | Deferred scope | Reason for deferral | Conditions for revisit |
|---|---|---|---|
| MAJ-12 (ACCEPTED) | Licensed or bespoke audio: commissioned scoring, licensed SFX libraries, an ambient music bed | The product deliberately does not make the expressive-personality investment; licensed material imports a provenance/licence owner that does not exist in this run, and comparator audio reuse is forbidden | GAME-172 records a material **Below** on a scored dimension that cues cannot close, **and** an owner accepts a provenance/licensing duty; then a bounded audio issue, not a GAME-133 edit |
| MAJ-14 (ACCEPTED) | Free-site funnel instrumentation beyond completion rate (attribution, returning-visitor measurement, share/waitlist conversion) | Requires cross-visit identity, which the locked session-scoped/no-PII posture forbids; the metrics were deleted with MAJ-1 | Family accounts ship (current explicit non-goal) and the privacy posture is re-decided in writing |
| MIN-17 (ACCEPTED) | DQ-4 pre-symbolic dot-face default for g12 | Needs curriculum input; affects the piece art system but blocks nothing in Gate C/D | Before the GAME-171 piece-family sign-off, with a named curriculum owner and date (or `Q-13` is scored without the tier) |
| MIN-18 (ACCEPTED) | Any **flip** of the Blender determination to "required" | The determination is settled now (not justified; optional provenance-tracked source-art only) and a flip needs evidence that does not exist | GAME-172 records a material `Below` on world/bridge visual cohesion that flat 2D authoring cannot close; the ADR in GAME-143 is amended |
| REVIEW §4 item 6 (W-6) | Formal child-data review artefacts: data-processing inventory, parental-consent surface analysis, named regime audit | No PII, no accounts and no server-side child data exist in this build; the launch-trust gates already own this question cross-project | CONSULTING-182 / CONSULTING-268 close, or any change that introduces accounts, UGC or server-side progress |
| REVIEW §4 item 7 (W-7) | A full SLO / error-budget programme and a crash-reporting owner for the static site | A static/edge Next.js page with session-scoped game state has no service SLO surface; the operational controls that matter (invariant and renderer-init alerting) are in scope via MAJ-19 | The game gains any server-side rendering of play state, or platform-level SLOs become an owner requirement |
| REVIEW §4 item 15 (W-15) | Locale-aware number/label formatting (`formatScaled` rounding, decimals at g56+, dot-face under OS text scaling) | The product is locked to en-US; adding format rules now would be unused configuration surface | A locale expansion decision (currently §13 non-goal) — then formatting becomes an AC on the engine's label path |

---

## 5. Residual Dissent

Items where a critique recommendation — or a branch of one — was overruled. Where an item is listed here, the parent item's disposition in §1/§2/§3 states what was accepted instead.

| ID | Reviewer's concern / recommended remedy | Why overruled |
|---|---|---|
| CRIT-2 | "Pick one position… **if the verbs ship**, name the approved contract-revision issue plus the engine legality/scoring telemetry changes" | The shipping branch is not available: the union is frozen at eight members, `PHASER_RENDERER_CONTRACT.md` v1.0.0 is a locked contract, and the engine has no subtract/split path. PRD §4.1 itself requires a separate approved issue for any change to legal actions, so shipping them inside this spec would be a contract rewrite by documentation. Only the deletion branch is lock-compatible. |
| CRIT-4 | Frames the fix as "replace the blanket claim with a conformance statement … **and name the conforming mode**, stating whether Relaxed build is the AA path" | Accepted, but with the clock explicitly retained: the reviewer's framing invites the reading that AA is recoverable by making the untimed mode the primary surface. The free-site timed round and the earned-break window are locked product law, so the resolution is per-SC conformance **with two recorded exceptions** and Relaxed as the *disclosed conforming alternative*, not as a replacement for the timed surface. |
| CRIT-5 | Treats the 23-vs-18 conflict as a count to be reconciled (implying the table should win) | Overruled in direction: the count is reconciled **upward** by defining the five rows other accepted critiques require (Q-19…Q-23), so GAME-172's 23-dimension commitment is preserved and every Q-ID becomes citable. Reconciling downward to 18 would silently shrink the gate's evidence contract. |
| CRIT-9 | "define the WebGL-software rendering lane as an explicit AC (**new or reopened story**)" | The "new story" branch is rejected: it would duplicate real-render infrastructure already owned by GAME-166. The lane becomes an extended AC on GAME-166 with the child reopened. |
| MAJ-12 | "close Q7/DQ-6 with a recorded decision" while DQ-6 keeps the escape hatch "**unless original *scoring* is commissioned**" | The conditional is rejected. An unowned conditional is how the audio question re-opened three times; the cue set is frozen as final state and a commissioning request would require a new owner decision, not a standing exception. |
| MAJ-16 | "**Either** scope the mirror to presentation of the semantic surface…, **or** drop the 'can run the game alone' claim" | The first branch is rejected: the locked accessibility requirement is that the game is fully understandable and operable without the canvas, which means the DOM path must be able to run a full round. The correct fix is to specify the degraded mode, give it a budget and a suite, and test failover — not to remove the capability and leave the a11y mandate unmet. |
| MAJ-22 | "Either the card blocks the next puzzle (row 10 fails) or it does not (**then '5 s auto-dismiss' has no observable meaning**)" | The dichotomy is false. A non-blocking inline offer has an observable **lifetime** distinct from a transition gate: it can outlive the transition without blocking input. Resolving it this way keeps both the 1.5 s pacing rule and the offer, and gives the 5 s number a testable meaning. |
| MIN-4 | "Specify the harness **or drop the row** until the mode is owned" | The drop-the-row half is rejected: the memory/leak requirement is real and would otherwise vanish with the unbounded mode. The row is retained and re-scoped to the supported round shape with an explicit measurement protocol. |
| MIN-21 | "Move the bonus into the engine/scoring table…, **add a row for the offer rate**" | The relocation is accepted; the added row is rejected because the scorecard is fixed at `Q-01…Q-23` (CRIT-5) and adding a 24th dimension would change what GAME-172 commits to. The offer rate is asserted under `Q-10` from `second_build` telemetry. |

---

## 6. Owner Decisions [RECORDED 2026-09-17]

The entries below retain the options, recommendations, consequences and scope-lock analysis from the review. The
owner-approved record that follows supersedes the recommendations as the current policy state. Approval resolves
governance choices; it does not assert implementation, human evidence, conformance, rollback, or production
readiness. O-7 remains the closure blocker.

| # | Decision | Options | Recommendation | Consequence of each option | Blocks lock? |
|---|---|---|---|---|---|
| **O-1** | **Comparator demotion** — World of Goo 2 scored or registry-only (PRD Q2), and the rest of the registry | (a) Demote to registry-only; registry = Railway Hero (scored), Poly Bridge 3 (scored, interaction-scoped), DragonBox (scored on `Q-04`), Sushi Monster (scored on `Q-11`), Goo 2 + Bridge Constructor + Hungry Fish (registry-only). (b) Keep Goo 2 scored. (c) Demote and delete Goo 2 from the registry entirely. | **(a)** | (a) Gate E scores four referenced entries, each mapped to a dimension; expressive-personality scope creep is removed; the historical benchmark is retained as a registry row. (b) Gate E gains an entry with no numeric mapping — unfalsifiable, and it legitimises character/music investment the product rejects. (c) Loses the dated historical benchmark and breaks three artefacts' references; no gain over (a). | No — needed before **GAME-172** closes |
| **O-2** | **Accessibility standard level and its exception set** | (a) WCAG 2.2 AA asserted per SC, with SC 2.2.1 and 2.2.2 recorded as **not met under the essential-limit exception**, and Relaxed build disclosed as the untimed conforming path. (b) WCAG 2.1 AA (drops the 2.2-added criteria). (c) WCAG 2.2 AA as a blanket claim. | **(a)** | (a) The claim is falsifiable and the return contract survives; the cost is a published two-criterion exception and a discoverable untimed mode. (b) The repo's mixed statements persist, GAME-134's evidence set shrinks, and the 48 px floor still exceeds 2.5.8 — the standard is simply older than the build. (c) Unfalsifiable; GAME-134 could pass every check and still not be AA, which risks a false conformance report at a Pilot-0 trust gate. | No — needed before **GAME-134** closes; the signer is named under O-7 |
| **O-3** | **Session-cap variance** — one cap or band-specific caps | (a) Single engine constant: 90 s **or** 6 bridges, all bands, all surfaces (break is a fixed 90 s wall-clock window). (b) Band-specific shorter caps (e.g. g12 5 bridges / 75 s) proposed from prototyping. | **(a)** | (a) The locked brief is honoured, one constant is testable, comparator scoping and pacing assumptions hold, and `Q-11` is unambiguous. (b) Requires an explicit written supersession of the locked constraint, re-derivation of pacing, difficulty-by-band and the `Q-11` threshold, plus a second constant set for the break window; it also invalidates the "90 seconds or 6 bridges" copy shipped today. | No — but (b) makes the spec **NOT_READY** until written as an override |
| **O-4** | **CI enforcement timing vs Gate F** | (a) Exactness/a11y/import-boundary suites must enforce in CI before Gate F; the real-render swiftshader lane may land after Gate F under a dated, recorded owner waiver. (b) Every next-gen suite, including the real-render lane, must enforce before Gate F. (c) Gate F may pass with locally executed suites plus a recorded gap. | **(a)** | (a) Gate F's core claims become CI-enforced while the infrastructure-heavy lane gets an accountable exception; risk is a late-detected visual regression, mitigated by the smoke test. (b) Strongest evidence, but couples the cutover to a cross-project ticket (CONSULTING-305) the game team does not control — a scheduling deadlock risk. (c) Repeats today's provenance weakness (authored-and-locally-run evidence) on the exact gate that authorises retiring a renderer. **Approved policy:** the real-render lane must be green in CI before Gate F; a waiver may continue engineering but cannot close Gate F or authorize promotion. | No — needed before **Gate F / GAME-135** closes |
| **O-5** | **Blender** — required, optional or forbidden | (a) **Not justified as a required dependency**; permitted only as optional provenance-tracked source-art tooling, default vector/SVG/CSS 2D, no runtime 3D and no GLB. (b) Required for environment props. (c) Forbidden outright. | **(a)** | (a) Zero pipeline cost, keeps provenance overhead low and matches all three v1 documents; the exposure is a possible cohesion shortfall, which GAME-172 would surface. (b) Adds a build step, an export artifact class and provenance overhead for assets that must be flattened to 2D anyway; would be justified only by a recorded cohesion `Below`. (c) Simplest, but removes a cheap optional tool and forces a 2D-only answer before there is evidence either way. | No — the ADR pointer goes into the epic (GAME-143) |
| **O-6** | **Production promotion ownership** and **how long the React renderer is retained after Gate F** | (a) Promotion is an explicit **owner release action tracked as a dependency** (outside GAME-105); the React renderer is retained until GAME-143 records the rollback point **and** one release cycle passes with no `renderer_init_failure` alert. (b) Promotion is an AC inside GAME-135. (c) Renderer retired immediately on Gate F pass. | **(a)** | (a) Keeps a human accountable for a production promotion and gives the dual-renderer period a measurable exit condition (one clean cycle). (b) Couples an infrastructure action to a story's DoD and creates an ambiguous "done" for GAME-135. (c) Removes the rollback target while `renderer_init_failure` data is still young — the one path that can strand users. **Approved boundary:** games-site production-pointer promotion is a separate owner release action; later LevelBest promotion is separately approved. | No — the retention rule goes into GAME-135/143 text |
| **O-7** | **Human-gate owner/date** — the four human gates (Gates D/E), and the accessibility signer | (a) Name one accountable reviewer per gate with target dates now (manual VoiceOver/NVDA record for GAME-134; uncoached child playtest + phone/desktop evidence for GAME-172; same-day Railway Hero comparison and IP sign-off for GAME-39/GAME-172), and name the AA conformance signer. (b) Leave open. (c) Descope the uncoached child playtest and record the limitation. | **(a)** | (a) Gates D and E can close and the epic can reach its DoD; cost is scheduled human time. (b) Gates D/E stay permanently blocked regardless of engineering quality and GAME-105 cannot reach Done — the current state, now recorded as a DoD condition rather than a question. (c) Shortens the calendar but Gate E can no longer claim child-discoverability evidence: the epic must then say so explicitly and cannot claim to have met the comparator bar on that dimension. | **Yes for closure** — not for the epic rewrite; under (b) the epic stays open |
| **O-8** | **Sanction of the two bounded new issues** | (a) Sanction both: **BB-CONTRACT-1** — renderer contract **v1.1.0** additive revision (session deadline block, `renderSeed`, ordered unit-based slots/`fillOrder`, per-intent required-field declaration, with compat/downgrade path and no authority change); **BB-CONTENT-1** — curriculum catalogue of record + content-volume supply floor + decoy-honesty property test. (b) Decline both and accept the recorded gaps. (c) Sanction only BB-CONTRACT-1. | **(a)** | (a) The accepted resolutions that need a contract field (CRIT-6, MAJ-8, MAJ-15, MIN-9) become buildable and testable, and the 14/14 reachability claim becomes falsifiable; note that **creating these issues is not licence to change authority semantics** — combine/split and physics-as-authority stay out of contract. (b) The spec keeps the gaps documented; CRIT-6/MAJ-8/MAJ-15/MIN-9 remain prose, GAME-133/135 cannot claim determinism or geometry authority, and the determinism/legality ACs are dropped — a materially weaker Gate D/F evidence set. (c) Contracts close, but the "14/14 reachable" claim stays unfalsifiable at content scale. | No — but declining (a) weakens the CRIT-6/MAJ-8/MAJ-15/MIN-9 resolutions to recorded gaps |

**Recorded approval — 2026-09-17.**

- **O-1:** World of Goo 2 is registry-only and unscored; GAME-172 scores only mapped comparators; no expressive reuse.
- **O-2:** WCAG 2.2 AA is the target with per-success-criterion evidence and no blanket claim. Exceptions must be substantiated; an unmet applicable Level A/AA criterion prevents an AA claim. Relaxed is an untimed accessibility path, not yet a conforming alternate version.
- **O-3:** One engine-owned 90 s / 6-bridge cap; the earned break is a fixed 90 s wall-clock window.
- **O-4:** Core suites and real Phaser Chromium SwiftShader must be green in CI before Gate F. A waiver is engineering-only and cannot close Gate F or authorize promotion.
- **O-5:** Blender is optional provenance-tracked source-art tooling for flattened 2D only; no runtime 3D/GLB.
- **O-6:** games-site production-pointer promotion is a separate owner release action; LevelBest promotion is separate; retain DOM/React through exercised rollback and one clean cycle.
- **O-7:** Human-gate, WCAG, and rollback owners/dates must name the candidate, environment, and durable evidence; assignment alone is not approval.
- **O-8:** Both bounded follow-ups are sanctioned and tracked as GAME-294 / GAME-295; sanction and issue creation are not implementation or acceptance.

---

## 7. Epic-Text vs Child-AC changes

### 7.1 What must appear in the rewritten GAME-105 epic description

The epic body carries the following, written once and referenced (never restated) by the three v2 documents. Items marked Ⓟ require the stated ratification before their number is treated as final.

1. **Honest coverage statement** replacing "every capability is owned / no new children": the ownership map, the AC-edit list for GAME-134/135/166/171/172/39, the two sanctioned new issues (BB-CONTRACT-1, BB-CONTENT-1), and a labelled `epic text` list for the areas that are narrative-only (curriculum authority, personas, technology review, Blender ADR pointer, sequence).
2. **Product law block**: no ads/loot boxes/FOMO/loss-aversion/playtime leaderboards/child-vs-child competition; session-scoped storage only; no PII; no comparator reuse of any kind; nothing may present a timed surface as pressure.
3. **Session model, one authority**: `ROUND_CAP_SECONDS = 90`, `ROUND_CAP_BRIDGES = 6`, whichever first; the **engine owns the monotonic deadline and expiry**; the host supplies visibility/route events as inputs (the closed intent union is unchanged); hidden-tab crossing semantics and device-sleep treatment; save-at-expiry handshake.
4. **Surface availability matrix** (one table): free site (timed round, Relaxed available, summary reachable, nothing saved) · earned break (fixed 90 s **wall-clock**, Relaxed hidden, **no** setup/summary/"Play again"/free-build surface reachable, save + non-cancellable 3 s return) · lesson/teacher entry (same window, unlock card skipped) · Relaxed (unclocked, bridges-built progress) · **free-build shelf = explicit non-goal**.
5. **Return-contract law**: no extension affordance in any surface or state; pause budgets (free site cumulative 60 s; break wall-clock, disclosed); the return button returns early only; "no path, control, parameter or reload may restore a window."
6. **Authority restatement**: engine owns units/composition/legality/verdicts/scoring/progression/curriculum/session deadline; renderer presents and emits the frozen eight intents; magnet assist is visual-only; decorative layer is post-verdict, seeded, non-authoritative and removable; no renderer-derived geometry in the verdict or legality path.
7. **Interaction model incl. `submit`**: the loop with the Check-it path, `incorrectSubmit` restating lengths, 0 penalties, no judgement copy; three input paths converging on identical intents.
8. **Hint contract**: engine constants 20 s / 3 failed placements (Relaxed 40 s / 6 Ⓟ), L1–L3 ladder with cap, `Try one level easier?` at most once per round and never an automatic band change.
9. **Verdict vocabulary mapping table** (engine verdict → presentation state → telemetry result), including `overshoot`.
10. **The canonical scorecard `Q-01…Q-23`** with `Status` and `Ratifier` per row, the statement that this set is closed, and the rule that a `Below` needs bounded remediation and retest on the same row.
11. **Canonical telemetry event table** (one table), plus sink, session scope, retention Ⓟ, access, alert thresholds/owners, and the **P0–P3 defect taxonomy**.
12. **Canonical performance budget table** (per device class A/B/C, from TECH §9.1) including the **DOM-first/React-path rows** and the long-task budget; supported device and DPR matrix (DPR ≤3 supported, render scale capped at 2).
13. **Accessibility contract**: WCAG **2.2 AA asserted per SC**, with SC 2.2.1 and 2.2.2 recorded as not met under the essential-limit exception; Relaxed build as the disclosed untimed conforming path; mirror facts **F1–F7**; verification list (axe on DOM only, keyboard-only round, reduced-motion/grayscale/zoom, manual AT record with a named executor); the conformance signer; the 48 px floor and nearest-centre hit-area rule.
14. **Comparator registry table** (one table): entry, status (scored / registry-only / folded), dimensions benchmarked, what Meets/Exceeds means, dated observation, access/licence check, and the rule that an unmapped entry is not scored. Nine-criterion Railway Hero record labelled **current-generation only**.
15. **Educational goals**: curriculum authority, the 14 skills, the coverage object (supply floor ≥12 per skill × band × difficulty tier Ⓟ), the decoy-honesty property, round vs session definitions and sample minimums for the session signals, and the no-efficacy-claim rule.
16. **Privacy/safety**: no PII, no accounts for kids, session-only storage with graceful degradation when blocked, no third-party scripts, child-safe defaults, and the statement that cross-visit metrics are not claimed.
17. **Technology review with responsibility per technology**, including the **audio cue inventory** with per-cue provenance and the frozen "no bespoke/licensed music" position, the **Blender determination** (O-5) with its ADR pointer, Figma (file identity without credentials), and the testing/observability tooling.
18. **Ownership of the host-shell/return-contract surface**: production design = GAME-171 (extended AC, sole owner; NG-09b withdrawn); runtime contract + parity/containment/fault tests = GAME-135; GAME-133 explicitly **not** an owner of shell surfaces.
19. **Release/cutover**: flag values (`dom | phaser` + percentage), staged ramp with cohort definition and exit criteria, parity criteria list, exercised rollback, DOM retention rule (O-6), promotion ownership (O-6), and the **CONSULTING-305 extension with the explicit next-gen suite list** plus the CI timing decision (O-4).
20. **Contract versioning policy**: `BRIDGE_VIEW_MODEL_VERSION` + intent-union versioning, compatibility matrix (same or same-major+1 additive), skew → fail closed to `dom` with `renderer_version_skew`, visible retry for a dropped intent, and the frozen-contract change route (BB-CONTRACT-1, additive only, no authority change).
21. **DoD conditions**: human-gate owners/dates recorded (O-7); no authored-but-unexecuted check described as passing; the AC-edit list executed; a `Below` remediation closed or explicitly deferred with rationale; **no claim of WCAG conformance beyond the recorded per-SC statement**.
22. **Jira hygiene actions restated as dependencies, not product work**: reparent GAME-39 and GAME-166 to GAME-105; return GAME-105 from Verification to In Progress; retire GAME-36 to Wont Do preserving its 12 Done stories as current-generation history; reopen/extend GAME-166.

### 7.2 Existing children whose acceptance criteria must be extended

| Child | What must be added to the AC | Source items |
|---|---|---|
| **GAME-171** (production Figma art direction, game-feel, responsive visual system) — **gains the host-surface clause and becomes the single owner** | "Production design covers the **host surfaces as one coherent set**: `/games` hub card, S0 setup (band cards, auto-band chip, session sentence, sound + Relaxed toggle), S3 summary (headline, boxes, coaching line, buttons, savings footer), S5 pause/settings, S4 earned-break countdown + saved state + return, S6 recovery/fallback. No frame may contain an extension, skip or 'more time' affordance. The countdown state sequence is specified with non-colour carriers (segment drop + numeric text + copy change at ≤10 s; steady warm amber; never pulsing/red/ticking). The crossing subject is the abstract non-character load marker with an IP provenance entry. Audio mix ceiling and mute variants per cue. Every frame passes the still-frame legibility check with a named design-QA owner. The 20 state names map 1:1 to named production frames (verified against the Figma file, not asserted)." | CRIT-1, CRIT-7, CRIT-8, CRIT-10, MAJ-12, MAJ-21, MAJ-24, MIN-13, MIN-17, W-16 |
| **GAME-133** (full piece/bridge/feedback/crossing/environment presentation migration) | "Consume the **ordered, unit-based** placement fields from the view-model (`slots`, `openSlots`, `fillOrder`) and never derive slot geometry from pixels; the decorative/crossing layer **may not emit intents**, is deterministic for a given `renderSeed`, and collapses to the final frame under reduced motion; cue inventory implemented per the epic audio table with per-cue provenance. **No host-shell surfaces** (hub/setup/summary/pause/return) are in scope — GAME-171 owns their design and GAME-135 their runtime contract. No free-build-shelf state families are built (non-goal)." | CRIT-1, CRIT-2, MAJ-8, MAJ-12, MAJ-15, MAJ-23, MAJ-24 |
| **GAME-134** (accessibility, responsive touch, exactness qualification) | "Evidence is produced against **WCAG 2.2 AA asserted per success criterion**, with the conformance table (met / not met + exception) as the deliverable and SC 2.2.1 / 2.2.2 recorded as not met under the essential-limit exception; the semantic mirror exposes **F1–F7** including remaining time and pause state with the stated announcement cadence (60/30/10 s + pause/resume, polite; assertive only for the return prompt); keyboard/SR assertions are driven **from the DOM mirror**, never from the canvas; the keyboard path includes **submit/Check it**, `placementPreview` and drag-equivalent operation; reduced-motion, grayscale, 200 %-zoom and 48 px/nearest-centre hit-area assertions are included; the manual VoiceOver/NVDA record carries a **named executor and date**, or Gate D is recorded as blocked; the hint-threshold e2e asserts the engine constants." | CRIT-4, CRIT-7, CRIT-10, CRIT-12, MAJ-3, MAJ-5, MAJ-18, MAJ-25, MIN-2, MIN-16 |
| **GAME-135** (performance, exact parity, e2e, rollout, rollback) | "**Import-boundary test**: no engine/session/exactness module imports Phaser, layout or `unitPx`; presentation/decorative modules cannot emit intents. **Frozen-contract gap record** for any field the v1.1.0 revision (BB-CONTRACT-1) adds, with the pre-revision behaviour specified. **Placement invariance test**: identical intent sequences under varying `snapTolerancePx`/`unitPx`/DPR yield identical placements and identical legal-action sets. **Session tests**: fake-clock + CDP page-hide + resume + tamper (no path restores a window), pause-budget assertion, break wall-clock expiry, save-at-expiry. **Return-contract containment test**: from the break entry no route/control/parameter reaches setup, summary, 'Play again'/'Change level', Relaxed or any free-build surface; 0 extension affordances. **Host-shell contract** implemented and parity-tested against the current-generation GAME-36 contracts, with `GameHostProps`/`GameHostEvents` and the expiry handshake. **Failure tests**: mid-round renderer failure (crash, WebGL context loss, dynamic-chunk failure, asset 404) with the DOM-first path, focus restoration and the announced fallback; version-skew; dropped-intent retry affordance. **DOM-first budget and a11y/exactness suite** plus the React-path budget rows; the parity criteria list (exactness, a11y, telemetry/privacy, lifecycle, performance) recorded before DOM retirement. **Canonical telemetry table** with sink/retention/access, alert thresholds/owners and the P0–P3 taxonomy. **DoD** carries: the CONSULTING-305 extension (explicit suite list) or a dated owner waiver (O-4); exercised rollback; the renderer retention rule (O-6)." | CRIT-1, CRIT-6, CRIT-7, CRIT-9, CRIT-10, MAJ-7, MAJ-8, MAJ-10, MAJ-14, MAJ-16, MAJ-17, MAJ-19, MAJ-20, MIN-4, MIN-9, MIN-10, MIN-12, MIN-20, W-1, W-8, W-11, W-17 |
| **GAME-172** (external benchmark polish, remediation, child/device quality gate) | "Score exactly the canonical dimensions **`Q-01…Q-23`** (no other list, no re-derivation) on the production candidate, against the **comparator registry table** with per-entry status and dimension mapping; record the access/licence check for every scored entry and treat lost access as a recorded Gate E limitation; remediation is bounded to rows marked `Below` with retest on the same row; child/device evidence carries a named executor and date or the limitation is stated; **no comparator asset, name, layout, audio or writing is reused**; the session-signal sample minimums are applied and no efficacy claim is made." | CRIT-5, CRIT-11, MAJ-1, MAJ-6, MAJ-11, MAJ-26, MIN-8, MIN-21 |
| **GAME-166** (real Phaser 4 rendering smoke test — reopen / extend) | "Reopen: the deliverable is not complete with the jsdom limitation documented. Add the real-render lane — Playwright chromium with `--use-gl=swiftshader` driving the real Phaser scene, asserting a rendered frame and failing when rendering breaks — with the test running in CI (CONSULTING-305 extension) or a dated owner waiver (O-4). The vitest alias-level canvas mock is **non-evidence** and may not be cited in a gate packet." | CRIT-9, MAJ-10, MIN-11 |
| **GAME-39** (current-generation T13 benchmark + IP sign-off) | "The nine-criterion Railway Hero record is labelled **current-generation only** and is never cited as Phaser/next-generation evidence; reparented to GAME-105; the human gates (same-day Railway Hero comparison with per-row evidence, uncoached child playtest notes, human IP sign-off) carry a **named owner and date** (O-7); the IP sign-off is paired with the per-asset provenance manifest reviewed at Gate G." | CRIT-3, CRIT-11, MAJ-13, MIN-8, W-6 |

**Other children, for completeness (no AC change required):** GAME-129 (fixtures; referenced by the `decoyHonesty` and supply-floor properties), GAME-130 (frozen contract owner of record — BB-CONTRACT-1 is its successor artefact), GAME-131 (host adapter; the host-shell contract is additive to it), GAME-132 (vertical slice; unchanged), GAME-143 (gains the Blender ADR, the dual-renderer history, the Figma identity without credentials, the rollback point and the provenance manifest that GAME-172/GAME-39 reference), GAME-147 (dependency only), CONSULTING-305 (extended scope, not a GAME story), CONSULTING-182/268 (Pilot-0 trust gates; W-6 deferral target).

---

## 8. Scope-Lock Decision

**Verdict: READY**

**Reasoning.** Every blocker is resolved (12 ACCEPTED, none deferred, none rejected) and each resolution is written as an instruction that changes a named document and/or a named child AC. The three failure classes the reviewer identified are all closed by decision, not by prose: the coverage-check dishonesty (CRIT-1/CRIT-3/CRIT-8/CRIT-9/CRIT-11) is closed by the ownership map, the AC-edit list and the two sanctioned issues; the locked-constraint violations in the bounded session and return contract (CRIT-6/CRIT-7) are closed by one monotonic authority, a bounded pause budget, a wall-clock break window and a non-extendable return, each with a test; and the unverifiable acceptance claims (CRIT-4/CRIT-5/CRIT-12) are closed by a per-SC conformance statement with two recorded exceptions, a closed 23-dimension scorecard with stable IDs, and one standard. The frozen contracts are intact: nothing in this log rewrites `RENDERER_BOUNDARY.md`, `PHASER_RENDERER_CONTRACT.md` v1.0.0 or the eight-member intent union; the additive field changes are routed through BB-CONTRACT-1 under O-8, and the two out-of-contract behaviours (combine/split; physics as authority) are declared non-goals rather than adopted.

**Three conditions attached to the verdict, none of which reopen the specification.**

1. **Owner decisions O-1…O-8 are recorded on 2026-09-17** (they gate child closure, not the epic rewrite). O-3 preserves the locked 90 s / 6-bridge constraint, and O-8 sanctions both bounded follow-ups. O-7 human evidence owners/dates and O-8 follow-up implementation remain open.
2. **Nothing may be reported as done that is authored-only.** The `[required work]` set is unchanged and now larger in name: production Figma frames (GAME-171), the manual AT record, uncoached child playtests, device evidence, the human IP sign-off, the real-render lane and the next-generation benchmark all remain required work with named vehicles.
3. **GAME-105 must not claim conformance or coverage beyond the recorded statements** — no blanket WCAG AA, no "every capability is owned", no "no gaps", no metric the privacy posture cannot compute.

---

## Appendix A — Dispositions for REVIEW §4 ("What's missing"), §5 (contradictions) and §6 (implicit assumptions)

**REVIEW §4 items (W-1…W-17).** W-1 mid-round renderer failure → ACCEPTED (MAJ-16 ACs + fault test). W-2 timer semantics/paused boundary → ACCEPTED (CRIT-6/CRIT-7; single written rule). W-3 audio specification → ACCEPTED (MAJ-12 cue inventory + provenance + `Q-20`). W-4 telemetry sink/retention/access/deletion → ACCEPTED (MAJ-19; deletion collapses to "session-scoped client storage; no PII to delete server-side"). W-5 content authoring and volume → ACCEPTED (MAJ-11; BB-CONTENT-1). W-6 child-data/safety regime → **ACCEPTED in part / DEFERRED in part** (regime statement and no-consent-surface note go into the epic text; the formal review artefacts defer to CONSULTING-182/268 — §4). W-7 operations/SLOs → **ACCEPTED in part / DEFERRED in part** (alerting thresholds and owners via MAJ-19; the SLO/error-budget programme deferred — §4). W-8 rollout cohort/ramp/exit criteria → ACCEPTED (epic text item 19; GAME-135 AC). W-9 the 23 dimensions → ACCEPTED (CRIT-5). W-10 zero/one-bridge and timeout summary states → ACCEPTED (GAME-171 AC adds 0-bridge, 1-bridge, clock-expiry-on-puzzle-1 and no-hint-free-solve copy, all under the no-shaming rule). W-11 `Change level` semantics → ACCEPTED (write once: returns to S0 preserving nothing; **unavailable in the break**, which has no summary; free site only). W-12 accessibility of the timer and the fallback path → ACCEPTED (MAJ-25 F7 + cadence; MAJ-16 failover announcements + reduced-motion return behaviour). W-13 200 %-zoom fitted-span operation → ACCEPTED (MAJ-18 pan-as-a-unit rule + hit-area rule). W-14 earned-break component/API surface → ACCEPTED (MAJ-20). W-15 locale/number formatting → **DEFERRED** (§4). W-16 design-QA process owner → ACCEPTED (GAME-171 AC: named design-QA owner for the still-frame check). W-17 import/authority test ownership → ACCEPTED (CRIT-9: GAME-135 AC).

**REVIEW §5 contradictions (1–22).** All 22 are resolved by the item they map to, in the order printed in the review: (1) CRIT-12, (2) CRIT-4, (3) CRIT-6, (4) CRIT-7, (5) MAJ-21, (6) MAJ-3, (7) CRIT-10, (8) CRIT-2, (9) CRIT-5, (10) CRIT-11, (11) MIN-3 + MAJ-18, (12) MAJ-18, (13) MAJ-18, (14) MAJ-24, (15) CRIT-1 + MAJ-23, (16) CRIT-8, (17) CRIT-3 + CRIT-8 + CRIT-1, (18) CRIT-3, (19) CRIT-1 + CRIT-7, (20) MIN-18, (21) MAJ-22, (22) MAJ-19 + MIN-15. No contradiction is left standing in v2: each is either fixed in all three documents or becomes a recorded decision with a single owner.

**REVIEW §6 implicit assumptions (1–18).** Each is converted into an explicit statement, an AC, or a deleted claim: 1 → CRIT-10 (submit first-class; `incorrectSubmit` reachable); 2 → MAJ-7 (visual-only magnet; invariance test); 3 → CRIT-6 (monotonic engine deadline; test, not scan); 4 → CRIT-7 (bounded pause budget; wall-clock break); 5 → CRIT-1 (non-goal + containment test); 6 → MAJ-6 (round/session definitions, sample minimum, rise claim restricted); 7 → MAJ-2/MIN-6 (generation boundary stated; every `[existing]` cited); 8 → CRIT-11 (comparator↔dimension mapping; unmapped entries unscored); 9 → MAJ-16 (DOM-first budget, suite, failover UX); 10 → MAJ-17 (compat matrix, skew detection, visible retry); 11 → MAJ-18 (hit-area cap + nearest-centre rule); 12 → MAJ-18 (proportionality invariant + legibility floor + pan rule); 13 → MAJ-20 (published host contract); 14 → MAJ-13/O-7 (owner/date recorded as a DoD condition); 15 → MIN-6 + PRD §7 (blocked-storage degradation stated; no UI difference implies a stated no-op, asserted in tests); 16 → MAJ-3 (one engine constant set); 17 → CRIT-8 (GAME-171 AC extension is now the mechanism, with a named editor action and the sequencing rule); 18 → CRIT-11 (access/licence check recorded per scored entry; loss of access becomes a recorded Gate E limitation).


